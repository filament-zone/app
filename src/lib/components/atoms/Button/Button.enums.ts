export enum EButtonSizeVariant {
	PRIMARY = 'primary',
	FULL_WIDTH = 'full-width'
}

export enum EButtonStyleVariant {
	PRIMARY = 'primary',
	SECONDARY = 'secondary',
	DISABLED = 'disabled',
	POSITIVE = 'positive',
	NEGATIVE = 'negative',
	HIGHLIGHT = 'highlight'
}

// Keep EButtonColorVariant for backwards compatibility if needed
export enum EButtonColorVariant {
	PRIMARY = 'primary',
	SECONDARY = 'secondary',
	HIGHLIGHT = 'highlight',
	POSITIVE = 'positive',
	NEGATIVE = 'negative',
	YELLOW = 'yellow',
	BLACK = 'black'
}
